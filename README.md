# Pure Sudoku

A calm, client-side 6x6 Sudoku game with difficulty levels, daily streaks, hints, undo, notes, and a solve timer. Progress is saved automatically so you can resume where you left off.

## Development

You need Node.js and npm — [install with nvm](https://github.com/nvm-sh/nvm#installing-and-updating).

```sh
git clone <this-repository-url>
cd <repository-name>
npm i
npm run dev
```

## Scripts

- `npm run dev` — start the development server
- `npm run build` — production build
- `npm run preview` — preview the production build
- `npm run lint` — run ESLint
- `npm run format` — run Prettier

## Built with

- TanStack Start
- TypeScript
- React
- Tailwind CSS